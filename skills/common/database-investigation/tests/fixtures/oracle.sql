CREATE TABLE INVESTIGATOR.PARENT (A NUMBER(10), B NUMBER(10), PRIMARY KEY (A,B));
CREATE TABLE INVESTIGATOR.CHILD (A NUMBER(10) NOT NULL, B NUMBER(10),
  CONSTRAINT FK FOREIGN KEY (A,B) REFERENCES INVESTIGATOR.PARENT(A,B));
CREATE INDEX CHILD_IX ON INVESTIGATOR.CHILD(A,B);
